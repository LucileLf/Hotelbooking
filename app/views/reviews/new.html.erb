<div class="container">
  <%= simple_form_for [@booking, @review] do |f| %>
    <%= f.input :rating, label: "Note sur 5"%>
    <%= f.input :comment, label: "Dites-en nous plus!" %>
    <%= f.button :submit, "Confirmer" %>
  <% end %>
</div>
